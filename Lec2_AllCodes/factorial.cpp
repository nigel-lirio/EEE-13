#include <bits/stdc++.h>

using namespace std;

int fact(int n){
    // We can set the base case to be 0! = 1.

    // The recurrence is n! = n*(n-1)!.
}

int main(){
    int n = 6;
    printf("%d! is %d.\n", n, fact(n));
    return 0;
}